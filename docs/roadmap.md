core-java/
├── pom.xml
└── src/main/java/com/example/core/
├── basics/
│   ├── variables/          # Primitive types, wrappers, immutability
│   ├── operators/          # Arithmetic, logical, bitwise
│   └── controlflow/        # if/else, switch, loops
│
├── collections/
│   ├── list/               # ArrayList, LinkedList
│   ├── set/                # HashSet, TreeSet
│   ├── map/                # HashMap, TreeMap
│   └── queue/              # PriorityQueue, Deque
│
├── streams/
│   ├── filtering/          # filter, distinct
│   ├── mapping/            # map, flatMap
│   ├── reducing/           # reduce, collect
│   └── grouping/           # groupingBy, partitioningBy
│
├── concurrency/
│   ├── threads/            # Thread, Runnable
│   ├── executors/          # ExecutorService, ScheduledExecutor
│   ├── futures/            # Future, CompletableFuture
│   └── synchronization/    # synchronized, locks, atomics
│
├── io/
│   ├── file/               # FileReader, BufferedReader, NIO
│   ├── serialization/      # Serializable, Object streams
│   └── networking/         # Sockets, HTTP basics
│
├── exceptions/
│   ├── checked/            # IOException, SQLException
│   ├── unchecked/          # NullPointerException, IllegalArgumentException
│   └── custom/             # Your own exception classes
│
├── generics/
│   ├── basics/             # Generic classes, methods
│   └── wildcards/          # ? extends, ? super
│
├── designpatterns/
│   ├── creational/         # Singleton, Factory, Builder
│   ├── structural/         # Adapter, Decorator, Proxy
│   └── behavioral/         # Strategy, Observer, Command
│
├── functional/
│   ├── lambdas/            # Syntax, functional interfaces
│   ├── methodrefs/         # :: operator
│   └── optionals/          # Optional usage, null safety
│
└── testing/
├── junit/              # JUnit basics
└── mockito/            # Mocking examples
